{
  "name": "crucible",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "check:connection": "tsx scripts/check-connection.ts"
  },
  "devDependencies": {
    "dotenv": "^16.4.7",
    "turbo": "^2.3.3",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
